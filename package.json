{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "start": "pnpm run dev:stack",
    "dev": "pnpm --filter @workspace/ziadah-landing dev",
    "dev:engine": "pnpm --filter @workspace/ziadah-engine dev",
    "dev:api": "pnpm --filter @workspace/api-server dev",
    "dev:stack": "concurrently -k -n api,web -c blue,magenta \"pnpm --filter @workspace/api-server dev\" \"pnpm --filter @workspace/ziadah-landing dev\"",
    "db:up": "docker compose up -d postgres",
    "db:down": "docker compose down",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "engines": {
    "node": ">=20.19.0"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  }
}
